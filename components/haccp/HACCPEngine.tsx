"use client"
// Full HACCP AI engine — rendered client-side
// The complete 3-step form + AI generation + results dashboard
// is loaded dynamically to keep the initial bundle small

export { default } from "@/components/haccp/HACCPEngineClient"
